<div class="page-wrap">
	<!-- start header -->
	<header class="main-header">
		<!--

		<div class="container logo-wrap">
			{{#if @blog.logo}}
			<a class="logo image-logo" href="{{@blog.url}}"><img src="{{@blog.logo}}" alt="{{@blog.title}}"></a>
			{{else}}
			<a class="logo text-logo" href="{{@blog.url}}">{{@blog.title}}</a>
			{{/if}}

		</div>			 -->
		<nav class="navbar navbar-default" id="main-navbar">
			<a href="{{@blog.url}}" class="navbar-brand" style="padding-top: 0px;padding-bottom: 0px;padding-right: 0px; z-index:1; position:relative;"><img alt="Page Forty" src="{{@blog.logo}}" width="180" height="50"  alt="{{@blog.title}}"></a>
			<div class="container">
				<div class="navbar-header">
					<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
					<span class="sr-only">{{t "Toggle navigation"}}</span>
					<span class="icon-bar"></span>
					<span class="icon-bar"></span>
					<span class="icon-bar"></span>
					</button>
					<!--<a href="#" id="search-open"><i class="fa fa-search"></i></a> -->
				</div>
				{{navigation}}
			</div>
		</nav>
		{{!> secondary-bar}}
	</header>
	<!-- end header -->